import 'package:flutter/material.dart';

class VisualizationSettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('可视化设置'),
      ),
      body: Center(
        child: Text(
          '可视化设置页面内容待完善',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}
